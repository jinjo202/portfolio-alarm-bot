# 포트폴리오 홀딩스 알람 봇 — 계획서

날짜: 2026-08-15 · 상태: 초안 (사용자 확인 대기)

## 목표

보유 종목(대부분 ETF·펀드)과 그 ETF들의 상위 구성종목에 대해, 주가에 영향을 줄 만한 이벤트를
매일 텔레그램으로 요약해서 받는다.

알림 대상 이벤트:
1. **배당** — 배당락일·지급일·배당 발표/증액·삭감
2. **실적** — 실적 발표 일정(D-3 예고) + 발표 결과 요약 (ETF 상위 구성종목 기준)
3. **주요 뉴스** — 투자유치, M&A, 가이던스 변경, 규제 등 주가에 영향 미칠 뉴스
4. **영향력 있는 신규 리포트** — 증권사/기관 리포트 발췌 요약 (2단계)

## 전제 확인 사항 (사용자 답변 필요)

| # | 질문 | 배경 |
|---|------|------|
| 1 | **보유 종목 리스트를 어떻게 받을까?** | 대시보드(pf-dash)가 클라이언트 측 암호화라 봇이 직접 못 읽음. 옵션: (a) 티커 리스트를 `holdings.json`으로 직접 관리 (b) 대시보드에 봇용 비암호화 티커 목록 엔드포인트 추가 (c) 엑셀 파일을 봇 저장소에 함께 둠. **추천: (a)** — 종목 변경이 잦지 않으면 가장 단순 |
| 2 | **어디서 돌릴까?** | (a) GitHub Actions cron — 무료, 서버 불필요, 추천 (b) Vercel cron — pf-dash와 같은 인프라 (c) 기존 cron 서버 (compute_corr_vol.py 돌리는 곳이 있다면 거기에 추가가 제일 lazy) |
| 3 | **알림 빈도** | 추천: 매일 아침 07:30 KST 다이제스트 1회. 장중 속보까지 원하면 2단계에서 추가 |
| 4 | **텔레그램 봇 생성** | @BotFather로 봇 생성 → 토큰 발급 → 본인 chat_id 확인. 사용자가 직접 1회 수행 필요 (5분) |

## 아키텍처 (lazy 버전)

```
holdings.json (티커 + 유형)
        │
        ▼
bot.py  ── 매일 1회 cron ──────────────────────────────┐
  1. ETF 룩스루: 각 ETF의 상위 10 구성종목 조회         │
     (yfinance funds_data / 네이버금융 — 주 1회 캐시)   │
  2. 이벤트 수집 (감시 유니버스 = 직접보유 + 룩스루 종목) │
     - 배당: yfinance 배당 캘린더 (ex-date, 지급일)     │
     - 실적: yfinance earnings calendar (미국)          │
             + DART 공시 API (한국, 무료)               │
     - 뉴스: Google News RSS (티커별) + 네이버 뉴스 API │
  3. 필터·요약: Claude API 1콜                          │
     "주가에 영향 줄 이벤트만 골라 한국어로 요약"        │
  4. 중복 제거: sent.json (보낸 기사 URL/이벤트 키 기록) │
  5. 발송: Telegram Bot API sendMessage                 │
        ▼
텔레그램 다이제스트 (아침 1건)
```

**파일 구성 (총 3~4개):**
- `holdings.json` — 티커, 이름, 유형(ETF/개별주), 시장(KR/US/EU)
- `bot.py` — 수집 + 필터 + 발송 전부 (단일 스크립트, ~300줄 예상)
- `sent.json` — 발송 이력 (중복 방지, 자동 생성)
- `.github/workflows/daily.yml` — cron (GitHub Actions 선택 시)

**환경변수 (secrets):** `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`, `ANTHROPIC_API_KEY`, (한국 공시용) `DART_API_KEY`

## 데이터 소스 선정

| 데이터 | 소스 | 비용 | 비고 |
|--------|------|------|------|
| ETF 구성종목 (미국) | yfinance `funds_data.top_holdings` | 무료 | 상위 10개 제공 — 목적에 충분 |
| ETF 구성종목 (한국) | 네이버금융 ETF 페이지 파싱 | 무료 | KODEX 등. 자산운용사 CSV 대안 |
| 배당 (미/글로벌) | yfinance 캘린더 | 무료 | ex-date, 배당액 |
| 실적 일정·결과 (미국) | yfinance earnings dates | 무료 | EPS 서프라이즈 포함 |
| 실적·공시 (한국) | DART Open API | 무료 (키 발급) | 실적공시·주요사항보고 |
| 뉴스 (글로벌) | Google News RSS (`news.google.com/rss/search?q=TICKER`) | 무료 | 키 불필요, 가장 lazy |
| 뉴스 (한국) | 네이버 검색 API | 무료 (키 발급) | 한국 종목 커버리지 우수 |
| 요약·필터링 | Claude API (Haiku 4.5) | 하루 1콜, 월 ~$1 미만 | 노이즈 필터가 핵심 가치 |
| 증권사 리포트 | 네이버금융 리서치 / 한경컨센서스 | 무료 (파싱) | **2단계** — PDF 발췌는 별도 작업 |

skipped: 유료 뉴스 API(NewsAPI, Finnhub 유료), 자체 DB, 웹 대시보드 — RSS+무료API로 시작하고 커버리지가 부족하면 그때 추가.

## 텔레그램 메시지 형식 (예시)

```
📊 포트폴리오 모닝브리프 — 8/15 (금)

💰 배당
· MSFT (DFIV 구성): 8/21 배당락, $0.83 (+10% 증액)

📈 실적
· NVDA (QQQ 12%): 8/27(수) 장마감 후 발표 예정 (D-12)
· 삼성전자 (KODEX200 22%): 2Q 확정실적 공시 — 영업익 10.4조 (컨센 +5%)

📰 주요 뉴스
· TSMC: 애리조나 2공장 조기 가동 발표 — AI 수요 대응 (Reuters)

📑 신규 리포트
· 미래에셋 "반도체 — 메모리 사이클 재점화" (KODEX 반도체 관련) …발췌 3줄
```

## 단계별 계획

**1단계 (MVP, 반나절):** holdings.json 수동 작성 → 뉴스 RSS + yfinance 배당/실적 → Claude 필터 → 텔레그램 일일 다이제스트. 여기까지만 해도 요구사항 1·2·3 충족.

**2단계 (+반나절):** DART 공시 연동(한국 실적·주요사항), 네이버 뉴스 API, ETF 구성종목 자동 갱신(주 1회).

**3단계 (선택):** 증권사 리포트 수집·발췌, 장중 속보 알림(별도 cron 2~4시간 간격), 실적 발표 당일 결과 즉시 알림.

## 이 계획에서 뺀 것 (YAGNI)

- DB — `sent.json` 파일이면 충분. 알림 이력이 수천 건 넘으면 SQLite로.
- 웹 UI / 설정 페이지 — holdings.json 직접 편집.
- 실시간 감시 — 일 1회 다이제스트로 시작. 필요성이 확인되면 추가.
- 전체 ETF 구성종목(수백 개) 감시 — 상위 10개면 "비중 큰 종목" 요구에 부합.

## 다음 스텝

1. 위 표 1~3번 답변 (종목 리스트 소스 / 실행 위치 / 빈도)
2. @BotFather 봇 생성 후 토큰 전달 (환경변수로만 저장)
3. 승인 시 1단계 구현 착수
